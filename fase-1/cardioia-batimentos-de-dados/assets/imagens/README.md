# Imagens de ECG sintéticas

O conjunto completo contém 100 arquivos `.png`, um `manifest.json` e um aviso de uso. Devido ao volume, o arquivo compactado será hospedado no Google Drive e ligado no README principal.

As imagens foram geradas por `scripts/gerar_ecgs_sinteticos.py` com semente `20260809`. Elas não são exames reais e não possuem validade diagnóstica.

Antes da entrega:

- inserir o link público;
- testar o download em janela anônima;
- confirmar 100 PNGs;
- preservar o manifesto e o aviso de uso.
